<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.windwardline.levelflow-cache-topup</string>
  <!-- Pinned through wl-repo-script since 2026-09-20, as the minute bank and
       both backups are. This named a path inside the shared checkout, so the
       07:00 run executed whatever branch a concurrent session had out.

       LEVELFLOW_CHECKOUT is not optional and not decoration. The extracted
       tree carries code and none of the checkout's ignored data, and this job
       touches three pieces of it: the 7.7 GB calibration cache, the FMP usage
       ledger and the circuit marker. Unnamed, the cache would resolve to an
       empty directory in the temp tree and the sweep would warm the whole
       roster into it from nothing, up to its 2 GiB ceiling, every night. The
       script refuses a missing cache and checkoutState.ts refuses a missing
       checkout, so a wrong value here fails loudly instead. -->
  <key>ProgramArguments</key>
  <array>
    <string>/bin/zsh</string>
    <string>-lc</string>
    <string>LEVELFLOW_CHECKOUT=/Users/peacock/Projects/levelflow-cloud /Users/peacock/.local/bin/wl-repo-script /Users/peacock/Projects/levelflow-cloud scripts/ops/daily-cache-topup.sh</string>
  </array>
  <key>StartCalendarInterval</key>
  <dict>
    <key>Hour</key>
    <integer>7</integer>
    <key>Minute</key>
    <integer>0</integer>
  </dict>
  <key>RunAtLoad</key>
  <true/>
  <key>StandardOutPath</key>
  <string>/Users/peacock/Library/Logs/levelflow-cache-topup.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/peacock/Library/Logs/levelflow-cache-topup.log</string>
</dict>
</plist>
