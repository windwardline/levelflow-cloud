G1 guard both-sides only (|| -> &&): KILLED (4 failing: covers the OTHER fold too: an empty select fold is already no verdict, at both grains | publishes no select delta when the BASELINE side of the select fold is empty | a fold with no evidence is NO VERDICT, never a measured failure | writes null, not a flattering number, for select deltas over an empty select fold)
G2 drop the baseline side of the guard: KILLED (1 failing: publishes no select delta when the BASELINE side of the select fold is empty | a fold with no evidence is NO VERDICT, never a measured failure)
G3 drop the variant side of the guard: KILLED (3 failing: covers the OTHER fold too: an empty select fold is already no verdict, at both grains | a fold with no evidence is NO VERDICT, never a measured failure | writes null, not a flattering number, for select deltas over an empty select fold | prints a dash in both select-delta columns over an empty select fold)
G4 thinness instead of emptiness (=== 0 -> <= 1): KILLED (1 failing: treats ONE select fill as evidence: the guard is emptiness, not thinness | a fold with no evidence is NO VERDICT, never a measured failure)
G5 expectancy delta back to absent-as-zero: KILLED (4 failing: covers the OTHER fold too: an empty select fold is already no verdict, at both grains | publishes no select delta when the BASELINE side of the select fold is empty | a fold with no evidence is NO VERDICT, never a measured failure | writes null, not a flattering number, for select deltas over an empty select fold)
G6 printer prints 0.0 for an absent total delta: KILLED (1 failing: prints a dash in both select-delta columns over an empty select fold | a disposition the gate drew must survive to the people reading it)
G7 artifact drops the null key: KILLED (1 failing: writes null, not a flattering number, for select deltas over an empty select fold | a disposition the gate drew must survive to the people reading it)
G8 sigma 0 over an absent delta: KILLED (1 failing: covers the OTHER fold too: an empty select fold is already no verdict, at both grains | a fold with no evidence is NO VERDICT, never a measured failure)
F1 cellsTested counts graded cells: KILLED (2 failing: freezes the candidates the sealed read opened, and states the multiplicity the old freeze overstated | the empty-fold regrade sits beside the originals and moved no figure | excludes a no-verdict cell from cellsTested and from the expected false accepts | the freeze's multiplicity counts the cells the gate judged, never the cells it declined to judge)
F2 no refusal on an unknown disposition: KILLED (1 failing: refuses a cell whose disposition it cannot read, at both grains | the freeze's multiplicity counts the cells the gate judged, never the cells it declined to judge)
F3 no refusal on accepted AND noVerdict: KILLED (1 failing: refuses a cell marked accepted and noVerdict together | the freeze's multiplicity counts the cells the gate judged, never the cells it declined to judge)
F4 class cells counted without the judged filter: KILLED (3 failing: freezes the candidates the sealed read opened, and states the multiplicity the old freeze overstated | the empty-fold regrade sits beside the originals and moved no figure | refuses a cell whose disposition it cannot read, at both grains | counts judged class cells inside the axis prefix only)
F5 legacy freeze gains cellsGraded: KILLED (2 failing: re-derives a legacy freeze on the basis it was written with, and marks nothing it did not carry | the freeze's multiplicity counts the cells the gate judged, never the cells it declined to judge | re-derives the freeze from the tracked gradings, the class arms included | the sealed act-3 read's inputs are still the files it consumed)
F6 legacy freeze gains multiplicityBasis: KILLED (2 failing: re-derives a legacy freeze on the basis it was written with, and marks nothing it did not carry | the freeze's multiplicity counts the cells the gate judged, never the cells it declined to judge | re-derives the freeze from the tracked gradings, the class arms included | the sealed act-3 read's inputs are still the files it consumed)
D1 regrade a freeze-bound market arm IN PLACE: KILLED (4 failing: gives every pre-disposition grading a regraded copy and a stdout record, and holds nothing else | the empty-fold regrade sits beside the originals and moved no figure | binds every market arm's grading by its bytes | re-derives the freeze from the tracked gradings, the class arms included)
D2 regrade a freeze-input class grading IN PLACE: KILLED (2 failing: gives every pre-disposition grading a regraded copy and a stdout record, and holds nothing else | the empty-fold regrade sits beside the originals and moved no figure | every grading the freeze consumed predates the gate's disposition field | the sealed act-3 read's inputs are still the files it consumed)
D3 regrade the act-2 grading IN PLACE: KILLED (1 failing: gives every pre-disposition grading a regraded copy and a stdout record, and holds nothing else | the empty-fold regrade sits beside the originals and moved no figure)
D4 re-record arm S's artifactSha256 only: KILLED (2 failing: names the tracked freeze by its hash, in the read and in the ledger line | binds every market arm's grading by its bytes | the sealed act-3 read's inputs are still the files it consumed)
D5 re-record artifactSha256 AND recompute frozenHash: HARNESS ERROR Command failed: npx --no-install tsx -e 
    import { readFileSync, writeFileSync } from "node:fs";
    import { frozenH
D6 one figure moved in a regraded copy: KILLED (2 failing: changes no accepted flag and no figure, only dispositions, wording, and select deltas over an empty fold | freezes the candidates the sealed read opened, and states the multiplicity the old freeze overstated | the empty-fold regrade sits beside the originals and moved no figure)
D7 an accept flipped in a regraded copy: KILLED (2 failing: changes no accepted flag and no figure, only dispositions, wording, and select deltas over an empty fold | freezes the candidates the sealed read opened, and states the multiplicity the old freeze overstated | the empty-fold regrade sits beside the originals and moved no figure)
D8 flattering select delta left on an empty fold: KILLED (1 failing: changes no accepted flag and no figure, only dispositions, wording, and select deltas over an empty fold | the empty-fold regrade sits beside the originals and moved no figure)
D9 a retiring removal cell stops retiring in a copy: KILLED (1 failing: re-derives the register from the regraded removal arms exactly as from the originals | the empty-fold regrade sits beside the originals and moved no figure)
D5 re-record artifactSha256 AND recompute frozenHash: KILLED (3 failing: names the tracked freeze by its hash, in the read and in the ledger line | binds every market arm's grading by its bytes | re-derives the freeze from the tracked gradings, the class arms included | the sealed act-3 read's inputs are still the files it consumed)
