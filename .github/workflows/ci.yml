name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # `check` is the project's typecheck entry point (tsc -b --noEmit).
      - name: Run Typecheck
        run: npm run check

      - name: Run Lint
        run: npm run lint

      # Deliberate coverage split: unit/integration tests gate PRs here;
      # the Playwright e2e specs need production credentials and a live
      # deployment, so they run in deploy.yml after functions deploy and
      # BEFORE the frontend ships — a failing e2e halts the frontend
      # rollout and fails the deploy run loudly.
      - name: Run Tests
        run: npm run test

      - name: Build
        run: npm run build
